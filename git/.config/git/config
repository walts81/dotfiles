[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
[pull]
	rebase = true
[init]
  defaultBranch = master

# Include local user-specific settings (not tracked in repo)
[include]
  path = ~/.config/git/config.local
