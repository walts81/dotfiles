#alias ls='colorls'
alias lsa='ls -a'
alias l='ls -lh'
alias la='ls -lah'
alias rcat=$(which cat)
alias cat=$(which bat)
alias catx='cat -p'
alias ssh-srv='ssh jwalters@linux-srv1.home'
alias dvm='~/.dotnet/dotnet-install.sh'
alias apt='sudo nala'
function zshpl() {
  sed -i -E "s/plugins=(.*)\)/plugins=\1 $1\)/" ~/.zshrc
}
